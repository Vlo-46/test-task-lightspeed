export interface IProduct {
    id: number;
    name: string;
    price: number;
    originalImageUrl: string
}

export interface ICategory {
    id: number;
    name: string;
    originalImageUrl: string;
}